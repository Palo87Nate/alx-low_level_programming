#include <stdio.h>
#include "main.h"

/**
 * _islower - checks for lower case letter
 * @c : character to check the case#include <stdio.h>
 * Return: 0 or 1
 */
int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
